apiVersion: v1
kind: Service
metadata:
  name: {{ include "rest-locations.fullname" . }}
  labels:
    {{- include "rest-locations.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rest-locations.selectorLabels" . | nindent 4 }}
